# ---------------------------------------------------------------------------
# Python dependencies for the MLTP framework (port of FullModel_4EM_Suspension_FullTyre)
# Install with:   pip install -r requirements.txt
# ---------------------------------------------------------------------------

casadi>=3.6        # symbolic modelling, ca.Function, nlpsol('ipopt'), collocation helpers
numpy>=1.24
scipy>=1.10        # scipy.io  (loadmat / savemat),  scipy.signal.lfilter (moving average)
plotly>=5.18       # interactive HTML figures (replaces Simulink Data Inspector)
kaleido>=0.2.1     # OPTIONAL: static PNG/PDF export of Plotly figures (figure.write_image)

# ---------------------------------------------------------------------------
# Linear solver
# ---------------------------------------------------------------------------
# IPOPT ships *inside* the casadi wheel and uses the MUMPS linear solver, which
# needs no external library -- a solve runs out of the box on any platform.
# (The faster HSL/MA57 solver is deliberately not used: it requires a licensed
# HSL DLL that IPOPT loads at solve time. If you ever want it, set
# opts["ipopt"]["linear_solver"] = "ma57" and opts["ipopt"]["hsllib"] in
# userOpts.py and make the HSL library findable on your loader path.)
